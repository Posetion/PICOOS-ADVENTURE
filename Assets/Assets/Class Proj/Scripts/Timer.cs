/*using TMPro;
using UnityEngine;

public class Timer : MonoBehaviour
{
    public GameManager gameManager;

    private TextMeshProUGUI text;

    private void Awake()
    {
        text = GetComponent<TextMeshProUGUI>();
    }

    private void Update()
    {
        int timeLeft = gameManager.GetTime();
        text.text = $"Time Left : {timeLeft}s ";
    }
}


*/

